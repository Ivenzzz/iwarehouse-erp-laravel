* it is fully compatible with the current tech stack (Laravel + Inertia + React)
* use server side table joins instead of client side array joins
* all calculations should be done on the server instead of client
* all functionalities should be done on server side instead of client such as searching, sorting, filtering, export to csv, etc.
* remove the export tsv, copy tsv and export pdf. Only leave the Export CSV and Export XLSX. 
* Dont change the UI/UX of the whole page